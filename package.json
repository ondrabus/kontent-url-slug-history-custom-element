{
  "name": "url-slug-custom-element",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "esbuild app.ts --outfile=app.js --bundle --watch",
    "build": "esbuild app.ts --outfile=app.js --bundle"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@kentico/kontent-delivery": "^11.6.0"
  },
  "devDependencies": {
    "esbuild": "^0.14.27",
    "tslib": "^2.3.1",
    "typescript": "^4.6.2"
  }
}
